exports.up = async (database) => {
    <% if (d.tableName) { %>
    database.schema.table("<%= d.tableName %>", table => {
        // TODO
    });
    <% } %>
};

exports.down = async database => {
    <% if (d.tableName) { %>
    database.schema.table("<%= d.tableName %>", table => {
        // TODO
    });
    <% } %>
};
